# Brian McConahey — Portfolio

Static personal site (Home, Resume, Projects) for GitHub Pages.

Open `index.html` locally, or serve the folder:

```bash
python3 -m http.server 8765
```

Legacy Massively template assets are in `assets/_archive/` (not loaded).
